# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные

#### Интерфейс IProduct

Сущность, которая описывает товары в приложении приложении.
Содержит следующие сведения о товаре:
`id:string` - уникальный идентификатор товара
`title:string` - название товара
`image:string` - изображение товара
`category:string` - категория товара
`price:number|null` - цена товара, null означает, что товар бесценен
`description:string` - описание карточки товара.

#### Интерфейс IBuyer

Сущность, которая описывает данные о покупателе.
Содержит следующие сведения о покупателе:
`payment: TPayment` - способ оплаты: card - банковская карта, cash - наличные, '' - в случае если пользователь не выбрал способ оплаты
`address:string` - адрес доставки
`email:string` - электронная почта
`phone:string` - телефон.

### Модели данных

#### Класс ProductCatalog

Отвечает за хранение товаров, которые можно купить в приложении.

Поля:
`items: IProduct[]` - хранит массив товаров
`item: IProduct | null = null` - хранит выбранную карточку товара. По умолчанию нет выбранного товара (null).

Конструктор:
`constructor(protected events: IEvents){}` - которые генерируются при сохранении данных.

Методы класса:
`setItems(items: IProduct[]):void` - сохранение массива товаров полученного в параметрах метода
`getItems():IProduct[]` - получение массива товаров из модели
`getItemById(id: string): IProduct | undefined` - получение одного товара по его id
`setSelectedItem(item: IProduct): void` - сохранение товара для подробного отображения
`getSelectedItem():IProduct` - получение товара для подробного отображения.

#### Класс Cart

Хранит и управляет списком товаров, который пользователь выбрал для покупки.

Поля:
`items:IProduct[] = []` - массив товаров, выбранных пользователем для покупки. По умолчанию массив пустой, до добавления товаров, выбранных покупателем.

Конструктор:
`constructor(protected events: IEvents){}` - генерирует события при изменении состояния корзины.

Методы класса:
`getItems():IProduct[]` - Возвращает массив товаров, которые находятся в корзине
`add(item:IProduct):void` - добавление товара, который был получен в параметре, в массив корзины
`remove(product: IProduct):void` - удаление товара, полученного в параметре из массива корзины
`clear():void` - очистка корзины
`getTotalValue(): number` - Возвращает стоимость всех товаров в корзине
`getCount(): number` - Возвращает количество товаров в корзине
`hasProductById(id: string): boolean` - проверка наличия товара в корзине по его id, полученного в параметр метода.

#### Класс Buyer

Данные о покупателе, которые тот должен указать при оформлении заказа

Поля:
`payment: TPayment = ""` - способ оплаты
`address:string = ""` - адрес
`phone: string = ""` - телефон
`email: string = ""` - электронная почта.

Конструктор:
`constructor(){}`

Методы:
`setPayment(payment: TPayment):void` - устанавливает способ оплаты
`setAddress(address: string):void` - устанавливает адрес
`setPhone(phone: string):void` - устанавливает телефон
`setEmail(email: string):void` - устанавливает электронную почту
`getInfo():IBuyer` - возвращает объек с данными о покупателе
`clear():void` - очистка данных покупателя
`validOrder():TErrorsBuyer` - валидация данных формы Order. Возвращает объект со значением ошибок. Если же поле не содержит ошибок, то такое свойство в объекте будет отсутствовать.
`validContact():TErrorsBuyer` - валидация данных формы Contact. Возвращает объект со значением ошибок. Если же поле не содержит ошибок, то такое свойство в объекте будет отсутствовать.

### Слой коммуникации

#### Класс ApiClient

Класс отвечает за получение данных с сервера и отправку данных на сервер.

Поля:
`api: IApi` - объект, соответствующий интерфейсу IApi.

Конструктор:
`construcor(api:IApi){this.api = api}` - принимаем в конструктор объект, соответствующий интерфейсу IApi.

Методы:
`async getProducts(): Promise<TProductList> ` - делает GET-запрос на эндпоинт /product/ и возвращает объект, полученный от сервера
`async postOrderInfo(order: IOrderRequest): Promise<IOrderResponse>` - делает POST-запрос на эндпоинт /order/, передает на сервер объект с информацией о заказе, возвращает объект, подтверждающий покупку на определенную сумму.

### Слой представления

#### Класс Header

Класс управляет отображением шапки сайта.

Поля:
`counterElement: HTMLElement` - элемент отображения количества товаров в корзине.
`basketButton: HTMLButtonElement` - кнопка-иконка открытия корзины в шапке сайта.

Конструктор:
`constructor(protected events: IEvents, container: HTMLElement){}` - получает контейнер шапки сайта, находит кнопку корзины и элемент счетчика, регистрирует событие для открытия корзины .

Методы:
`set counter(value: number)` - устанавливает значение счетчика товаров в корзине.

#### Класс Gallery

Класс управляет отображением списка карточек товаров.

Поля:
`catalogElement: HTMLElement` - элемент для отображения списка карточек товаров.

Конструктор:
`constructor(container: HTMLElement){}` - получает контейнер для отображения списка карточек товаров.

Методы:
`set catalog(items: HTMLElement[])` - устанавливает список карточек товаров.

#### Класс Modal

Класс управляет отображением модального окна и его содержимого.

Поля:
`contentElement: HTMLElement` - контейнер для отображения контента.
`buttonClose: HTMLButtonElement` - кнопка закрытия модального окна.

Конструктор:
`constructor(protected events: IEvents, container: HTMLElement){}` - инициализирует модальное окно, находит контейнер для отображения контента в модальном окне и кнопку для закрытия модального окна, регистрирует событие для закрытия окна.

Методы:
`set content(item:HTMLElement)` - устанавливает элемент для отображения в модальном окне, заменяя предыдущий.

#### Класс OrderSuccess

Отображение информации об успешном оформлении заказа, итоговой сумме и предоставляет возможность вернуться на главную страницу.

Поля:
`amountInfo: HTMLElement` - элемент отображения суммы списанных средств.
`buttonNewOrder: HTMLButtonElement` - кнопка перехода на главную страницу.

Конструктор:
`constructor(protected events: IEvents, container: HTMLElement){}`- получает контент для отображения информации об успешном заказе, выбирает элемент, отображающий информацию о списанных средствах с баланса и кнопку по которой открывается главная страница с перечнем товаров.

Методы:
`set amount(value: number){}` - устанавливает значение для отображения информации о списанных средствах с баланса.

#### Класс Card

Базовый класс карточки товара. Используется как родительский класс для специализированных вариантов карточек товаров.

Поля:
`titleElement: HTMLElement` - элемент отображения названия товара.
`priceElement: HTMLElement` - элемент отображения цены товара.

Конструктор:
`constructor(container: HTMLElement){}` - получает контейнер карточки и находит элементы отображения названия и цены товара.

Методы:
`set title(value: string){}` - отображение название карточки товара.
`set price(value: string){}` - отображение цену товара.

#### Класс CardCatalog

Специализированная карточка для списка товаров. Наследует базовый класс Card и расширяет его добавлением категории и картинки товара.

Поля:
`categoryElement: HTMLElement` - элемент отображения категории товара.
`imageElement: HTMLImageElement` - элемент отображения картинки товара.

Конструктор:
`constructor(container: HTMLElement, actions?: ICardActions){}` - инициализирует карточку каталога, получает контейнер компонента, находит элементы категории и изображения, а также регистрирует пользовательские действия.

Методы:
`set category (value: string){}` - устанавливает категорию товара.
`set image(value: string)` - устанавливает картинку карточки товара.

#### Класс CardFull

Отвечает за отображение карточки со всей информации о товаре. Наследует класс базовый класс Card.

Поля:
`descriptionElement: HTMLElement` - элемент отображения описания товара.
`buttonBuy: HTMLButtonElement` - кнопка добавления товара в корзину.

Конструктор:
`constructor(container: HTMLElement, actions?: ICardActions){}` - находит элементы отображения описания товара и кнопку добавления в корзину.

Методы:
`set description(value: string)` - устанавливает описание товара в карточке.
`set isInBasket(value: boolean)` - устанавливает значения для кнопки, в случае если товар уже в корзине.
`set isDisabled(value: boolean)` - блокирует кнопку добавления в корзину и меняеет ее текст на "Недоступно".

#### Класс CardBasket

Отображает карточку товара в корзине. Наследует класс Card и дополняет его информацией о порядковом номере товара в корзине и добавляет возможность удалить товар из корзины.

Поля:
`basketItemIndex: HTMLElement` - отображает порядковый номер товара в корзине.
`buttonRemove: HTMLButtonElement` - кнопка удаления товара из корзины.

Конструктор:
`constructor(container: HTMLElement){}` - получает контейнер компонента, находит элемент отображения номера товара в корзине и кнопку удаления товара из корзины.

Методы:
`set index(value: number)` - устанавливает порядковый номер товара в корзине.

#### Класс Basket

Класс отображения корзины товаров.

Поля:
`submitButton: HTMLButtonElement` - кнопка подтверждения заказа.
`totalPriceElement: HTMLElement` - отображает сумму заказа.
`listElement: HTMLElement` - элемент для отображения списка товаров в корзине.

Конструктор:
`constructor(container: HTMLElement){}` - получает контейнер компонента, находит элемент отображения суммы заказа и кнопку подтверждения заказа.

Методы:
`set totalPrice(value: number)` - устанавливает сумму заказа.
`set isDisabled(value: boolean)` - устанавливает статус кнопки в случае пустой корзины.
`set items(items: HTMLElement[])` - устанавливает список товаров в корзине.

#### Класс Form

 
Базовый класс формы. Содержит общую логику работу с формами.

Поля:
`submitButton: HTMLButtonElement` - кнопка отправки формы.
`errorElement: HTMLElement `- контейнер отображения ошибок валидации.

Конструктор:
`constructor(container: HTMLFormElement){}` - принимает контейнер компонента и находит элемент отображения кнопки отправки формы и контейнер отображения ошибок валидации

Методы:
`protected abstract onSubmit(): void` - абстрактный метод для генерации событий перехода к следующей форме.
`set valid(value: boolean)` - блокирует отправку формы
`set errors(value: string)` - отображает текст ошибки

#### Класс FormOrder

Отвечает за отображение формы для ввода данных для оформления заказа.

Поля:
`buttonCard: HTMLButtonElement` - отображение кнопки выбора оплаты картой
`buttonCash: HTMLButtonElement` - отображение кнопки выбора оплаты наличными.
`adressInput: HTMLInputElement` - отображение элемента ввода адреса доставки.

Конструктор:
`constructor(container: HTMLFormElement, protected events: IEvents){}` - получает контейнер компонента, находит элемент отображения кнопок выбора способа оплаты и ввода адреса, генерирует событие перехода к следующей форме.

 Методы:
`set address(value: string)` - обновляет значение адреса доставки.
`set payment(value: string)` - обновляет значение выбранного способа оплаты.

#### Класс FormContact

Класс отображения формы для заполнения контактной информации пользователем.

Поля:
`inputEmail: HTMLInputElement` - отображение элемента для ввода email  
`inputPhone: HTMLInputElement` - отображение элемента для ввода телефона

Конструктор:
`constructor(container: HTMLFormElement, protected events: IEvents){}` - получает контейнер компонента для отображения формы, находит элементы для ввода информации о телефоне и электронной почты пользователя, генерирует событие пререхода к следующей форме.

Методы:
`set email(value: string)` - обновляет значение адреса электронной почты пользователя
`set phone(value: string)` - обновляет значение телефона пользователя

### События

#### События классов Представления

`modal:close` - генерируется при закрытии модального окна.
`order:open` - генерируется для открытия первой формы оформления заказа. 
`order:open-next` - генерируется для перехода на следующую форму оформления заказа. 
`order:submit` - генерируется при нажатии на кнопку подтверждения заказа.
`basket:open` - генерируется при нажатии на кнопку корзины.
`payment:input` - генерируется при выборе спосбо платы.
`address:input` - генерируется при вводе адреса.
`email:input` - генерируется при электронной почты.
`phone:input` - генерируется при электронной почты.


#### Cобытия в Моделях данных

`buyer:changed` - генерируется при изменении данных покупателя.
`preview:open` - генерируется при выборе товара для просмотра.
`catalog:changed` - генерируется при получении/измении каталога товаров.
`card:select` - генерируется при выборе карточки товара.  
`basket:changed` - генерируется при добавлении/удалении товара из корзины.
